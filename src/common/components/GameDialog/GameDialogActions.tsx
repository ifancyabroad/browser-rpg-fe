import { DialogActions, styled } from "@mui/material";

export const GameDialogActions = styled(DialogActions)(({ theme }) => ({
	justifyContent: "flex-end",
	paddingTop: 0,
	marginTop: "-11px",
}));
